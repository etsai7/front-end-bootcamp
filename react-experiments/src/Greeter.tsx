// Component Function names should start with capital. Regular utility functions should start with lower case
// If its a component, we want it to return HTML
export default function Greeter () {
    return (
        <p>Hello, world from the Greeter component</p>
    )
}